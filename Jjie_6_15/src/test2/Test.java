package test2;

public class Test {
    public static  int longestOnes(int[] nums, int k) {
        int length=nums.length;
        if(k>=length){
            return length;
        }
        int ret=0;
        return ret;
    }
}
